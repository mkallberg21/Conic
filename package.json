{
  "name": "conic-platform",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run --workspace @conic/domain build && npm run --workspace @conic/contracts build && npm run --workspace @conic/api build && npm run --workspace @conic/web build",
    "dev": "npm-run-all --parallel dev:api dev:web",
    "dev:api": "npm run --workspace @conic/api dev",
    "dev:web": "npm run --workspace @conic/web dev",
    "lint": "npm run --workspace @conic/api lint && npm run --workspace @conic/web lint && npm run --workspace @conic/contracts lint && npm run --workspace @conic/domain lint",
    "typecheck": "npm run --workspace @conic/contracts typecheck && npm run --workspace @conic/domain typecheck && npm run --workspace @conic/api typecheck && npm run --workspace @conic/web typecheck"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "npm-run-all": "^4.1.5",
    "typescript": "^5.7.2"
  }
}
